import styled from 'styled-components';
import { size } from '../../../styles/devices';

export const MainContainer = styled.div``;
export const MainInnerContainer = styled.main`
  width: 100%;
  max-width: ${size.laptop};
  margin: 0 auto;
  height: 3000px;
`;
